package Creature

class Demon(
    name: String,
    attack: Int,
    defense: Int,
    health: Int,
    minDamage: Int,
    maxDamage: Int,
    speed: Int
) : Creature(name, attack, defense, health, minDamage, maxDamage,0 ,speed)